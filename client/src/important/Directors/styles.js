import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
    container: {
       margin: "52px 0 0 8.5% ",
       display: 'flex',
       alignItems: 'center',
       maxWidth: "100%"
   },
   CircularProgress: {
    margin: "122px 0 0 45.5% ",

   }
}));